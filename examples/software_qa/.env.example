# Optional. Set this to the same value exported in the terminal that runs
# `holo agent-api` so the MCP server attaches to that runtime instead of
# spawning its own (a spawned runtime inherits the MCP host's macOS TCC
# permission context — see the example README).
HAI_AGENT_RUNTIME_API_TOKEN=
